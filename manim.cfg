[CLI]
# Default for every render in this repo (override with CLI flags if needed)
quality = high_quality
frame_rate = 60
pixel_height = 1080
pixel_width = 1920
disable_caching = True
preview = False
background_color = #0B1020
